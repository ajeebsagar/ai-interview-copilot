# Core FastAPI dependencies
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0

# Azure OpenAI
openai==1.54.0
azure-identity==1.19.0

# Faster Whisper for local speech-to-text (CPU optimized)
# Note: On Windows, faster-whisper requires ffmpeg. For testing, we'll use a simpler approach.
# Install faster-whisper separately if needed: pip install faster-whisper
# faster-whisper==1.0.3

# WebSocket support
websockets==13.0

# Redis for session management (optional)
redis==5.0.8

# Image processing for vision API
pillow==10.4.0

# File handling
python-multipart==0.0.12
aiofiles==24.1.0

# Utilities
python-dotenv==1.0.1

# Logging and monitoring
structlog==24.4.0
python-json-logger==2.0.7

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.2

# Code quality
black==24.10.0
ruff==0.7.0
mypy==1.13.0
